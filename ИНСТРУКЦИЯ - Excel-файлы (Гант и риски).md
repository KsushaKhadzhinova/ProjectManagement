# Инструкция: что делать в Excel-файлах, какие там формулы и что именно скриншотить

Этот файл — подробный гайд по двум готовым Excel-файлам в этом репозитории:
`pz-wbs-gantt\Планировщик проекта - DiagramCode.xlsx` и `risk-management\Карта рисков - DiagramCode.xlsx`.
Данные и формулы в них уже заполнены — тебе нужно только открыть файлы в настоящем Excel (не в
Google Sheets и не в LibreOffice — условное форматирование графика Ганта и структурные ссылки
таблиц рассчитаны именно на Excel), убедиться, что цифры совпадают с тем, что описано ниже, и снять
скриншоты для отчёта.

---

## 0. Общие действия перед началом (для обоих файлов)

1. Открой файл двойным щелчком — откроется в установленном у тебя Microsoft Excel.
2. Если Excel показывает жёлтую плашку **«Защищённый просмотр»** — нажми **«Разрешить редактирование»**.
3. Если после этого показывается плашка **«Разрешить содержимое» / «Enable Content»** (из-за формул в
   структурированных таблицах) — нажми её тоже.
4. Сразу после открытия нажми **Ctrl+Alt+F9** — это команда «пересчитать всё заново», она гарантирует,
   что все формулы (включая структурные ссылки на `Таблица1`) пересчитались с нуля. Это безопасно и
   ничего не портит.
5. Если что-то выглядит не так, как описано ниже (например, показывает 0 или ошибку `#REF!`) —
   не паникуй и напиши мне, пришлю скриншот того, что вижу я, для сравнения.

---

## 1. Файл «Планировщик проекта - DiagramCode.xlsx» (WBS + график Ганта)

### 1.1. Из каких листов состоит файл

| Лист | Что на нём |
|---|---|
| **Пример 1** | Родной пример из методички («Обучение сотрудников кибербезопасности», 12 работ) — оставлен нетронутым, просто для справки, мы его не используем. |
| **Пример 2** | Ещё один родной пример из методички (про «Операция А/В/С…») — тоже не используем, оставлен как есть. |
| **ТАБЛИЦА ДАННЫХ** | Здесь я заполнила реальные 12 работ проекта DiagramCode и формулы метода критического пути (CPM). Это «мотор» файла. |
| **Планировщик проекта** | Сюда формулами подтягиваются данные с листа «ТАБЛИЦА ДАННЫХ», и условное форматирование само рисует полосы графика Ганта. Ничего вручную здесь вводить не нужно — если данные на «ТАБЛИЦА ДАННЫХ» верны, график нарисуется сам. |

### 1.2. Лист «ТАБЛИЦА ДАННЫХ» — что в каждом столбце и что означают формулы

Работы находятся в строках **8–19** (12 строк = 12 работ). Столбцы:

| Столбец | Заголовок | Что внутри | Формула (одинаковая логика во всех строках) |
|---|---|---|---|
| B | № | Номер работы (1–12) | Число, вписано вручную |
| C | ЗАДАЧА | Название работы | Текст, вписано вручную |
| D | ПРЕДШЕСТВУЮЩИЕ РАБОТЫ | Какие работы должны закончиться до старта этой | Текст-справка (например «2, 3»), на расчёт не влияет напрямую |
| E | ПОСЛЕДУЮЩИЕ РАБОТЫ | Какие работы начнутся после этой | Текст-справка |
| F | РАНН�ИЙ СТАРТ (ES) | Самый ранний день, когда работа может начаться | Если предшественников нет — `1`. Если один предшественник в строке R — `=H{R}`. Если несколько — `=MAX(H{R1},H{R2})` (берём самое позднее из окончаний предшественников) |
| G | ДЛИТЕЛЬНОСТЬ (D) | Сколько дней длится работа | Число, вписано вручную |
| H | РАННИЙ ФИНИШ (EF) | Когда работа закончится, если начать как можно раньше | `=F+G` (эта же строка) |
| I | ПОЗДНИЙ СТАРТ (LS) | Самый поздний день, когда можно начать работу, не сдвигая проект | `=J-G` (эта же строка, через структурную ссылку на таблицу) |
| J | ПОЗДНИЙ ФИНИШ (LF) | Самый поздний день, когда работа должна закончиться, не сдвигая проект | Если последователей нет (последняя работа) — `39` (общая длительность проекта). Если один последователь в строке R — `=I{R}`. Если несколько — `=MIN(I{R1},I{R2})` (берём самое раннее требование из последователей) |
| K | РЕЗЕРВ (TF, total float) | Насколько можно задержать работу, не сдвигая весь проект | `=I-F` (Поздний старт минус Ранний старт) |
| L | КРИТИЧЕСКИЙ ПУТЬ | «ДА», если резерва нет вообще (работу нельзя задержать ни на день) | `=IF(K=0,"ДА","НЕТ ")` |

Это классический метод **CPM (Critical Path Method / метод критического пути)**:
сначала считаем «слева направо» (F, H — прямой проход), потом «справа налево» (J, I — обратный
проход), разница между ранним и поздним стартом даёт резерв, а работы с нулевым резервом образуют
критический путь — самую длинную цепочку работ, которая определяет минимальный срок всего проекта.

### 1.3. Таблица со всеми 12 работами и правильными расчётными значениями

Открой лист «ТАБЛИЦА ДАННЫХ» и сверь, что у тебя получаются **ровно такие же числа** (Excel посчитает
их сам по формулам — ничего вводить дополнительно не нужно, просто сверься глазами):

| № | Работа | Пред. | Дл. (G) | ES (F) | EF (H) | LS (I) | LF (J) | Резерв (K) | Крит. путь (L) |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Требования и архитектура | — | 5 | 1 | 6 | 1 | 6 | 0 | ДА |
| 2 | Проектирование DSL-грамматики и модели данных | 1 | 4 | 6 | 10 | 6 | 10 | 0 | ДА |
| 3 | Настройка окружения разработки и CI | 1 | 2 | 6 | 8 | 10 | 12 | 4 | НЕТ |
| 4 | Реализация DSL-парсера (лексер, AST) | 2 | 6 | 10 | 16 | 10 | 16 | 0 | ДА |
| 5 | Модель данных и репозитории (SQLAlchemy) | 2, 3 | 5 | 10 | 15 | 12 | 17 | 2 | НЕТ |
| 6 | Нотация ERD: валидация и рендер | 4 | 5 | 16 | 21 | 16 | 21 | 0 | ДА |
| 7 | Аутентификация (JWT, bcrypt) | 5 | 4 | 15 | 19 | 17 | 21 | 2 | НЕТ |
| 8 | REST API диаграмм и версий | 6, 7 | 6 | 21 | 27 | 21 | 27 | 0 | ДА |
| 9 | IDE-оболочка: React + Monaco редактор | 3 | 7 | 8 | 15 | 20 | 27 | 12 | НЕТ |
| 10 | Канвас рендера, интеграция Run/Save/Export | 8, 9 | 5 | 27 | 32 | 27 | 32 | 0 | ДА |
| 11 | Тестирование (pytest + Jest, интеграция) | 10 | 4 | 32 | 36 | 32 | 36 | 0 | ДА |
| 12 | Подготовка и защита курсовой (РПЗ, демо) | 11 | 3 | 36 | 39 | 36 | 39 | 0 | ДА |

**Общая длительность проекта: 39 рабочих дней.**
**Критический путь (работы с резервом 0): 1 → 2 → 4 → 6 → 8 → 10 → 11 → 12.**
Работы 3, 5, 7, 9 в критический путь не входят — у них есть резерв (запас времени), значит их можно
слегка задержать без риска сорвать общий срок проекта.

Если хоть одна цифра в твоём Excel отличается от таблицы выше — значит формула съехала (например,
кто-то случайно стёр минус или изменил ссылку). Напиши мне номер строки/столбца — разберёмся.

### 1.4. Лист «Планировщик проекта» — как проверить график Ганта

1. Перейди на вкладку **«Планировщик проекта»** внизу.
2. В ячейке **J2** («Рассматриваемый период») стоит `60` — это ширина окна графика в днях; трогать не
   нужно, наш проект (39 дней) полностью в него помещается.
3. Столбец **C** (строки 5–16) должен показывать твои 12 названий работ — если видишь «раб1», «раб2»
   и т.п. — значит открыт не тот файл или данные не пересчитались (см. п. 0.4 выше, Ctrl+Alt+F9).
4. Начиная примерно со столбца **J** и дальше вправо — должны появиться закрашенные ячейки (полосы) —
   это и есть график Ганта. Каждая строка — одна работа, длина полосы — длительность, положение по
   горизонтали — с какого дня по какой идёт работа.
5. Прокрути/уменьши масштаб (правый нижний угол окна Excel, ползунок зума, поставь ~70%), чтобы видеть
   все 12 строк и все полосы целиком на одном экране.

### 1.5. Что и как скриншотить

1. Нажми **Win+Shift+S** (откроется инструмент вырезки Windows).
2. Выдели область: от строки 1 (заголовок «Планировщик проекта») до строки 16-17 (последняя работа),
   и по горизонтали — от столбца B до конца видимых цветных полос (обычно это где-то до столбца, где
   заканчивается 39-й день — с запасом можно чуть дальше).
3. Скриншот сохранится в буфер обмена — вставь его (Ctrl+V) в Paint или сразу сохрани через уведомление
   вырезки, выбрав **«Сохранить как»** → PNG.
4. Назови файл `Планировщик проекта - скриншот.png` и положи его в папку `pz-wbs-gantt\` этого
   репозитория (рядом с самим xlsx-файлом).

**Подпись под рисунком (подрисуночная подпись) для отчёта:**
> Рисунок 1 — График Ганта проекта DiagramCode (курсовой срез, Волна 1), построенный автоматически в
> Excel-планировщике по расчёту критического пути (CPM); общая длительность — 39 рабочих дней,
> критический путь выделен согласно условному форматированию столбца «Критический путь».

---

## 2. Файл «Карта рисков - DiagramCode.xlsx»

### 2.1. Из каких листов состоит файл

| Лист | Что на нём |
|---|---|
| **Приветствие** | Заставка от автора шаблона (сайт course-excel.ru) — не трогаем, не относится к заданию. |
| **Таблица с рисками** | Здесь я заполнила 9 реальных рисков проекта DiagramCode. Это «мотор» файла. |
| **Карта рисков** | Сюда автоматически подтягивается встроенная диаграмма рассеяния (scatter chart) — она сама рисует точки по данным с листа «Таблица с рисками», ничего вручную вводить не нужно. |

### 2.2. Лист «Таблица с рисками» — что в каждом столбце

Реальные данные — в строках **4–12** (9 строк = 9 рисков). Столбцы:

| Столбец | Заголовок | Что внутри |
|---|---|---|
| A | Номер риска | 1–9, вписано вручную |
| B | Обозначение риска | Автоматически формируется формулой `="R "&A&" "` → получится «R 1», «R 2» и т.д. — трогать не нужно |
| C | Наименование риска | Текст, вписано вручную |
| D | Влияние риска на проект/предприятие | Текст, вписано вручную |
| E | Что является триггером для проявления риска | Текст, вписано вручную |
| F | Оценка вероятности возникновения риска | Число от 0 до 1 (0,2 = редко, 0,6 = довольно часто) |
| G | Оценка силы воздействия риска | Число от 0 до 1 (0,3 = слабо, 0,9 = очень сильно) |
| H | Итоговый коэффициент | Формула `=F*G` — считается автоматически, чем выше — тем приоритетнее риск |
| I | Мероприятия по минимизации рисков | Текст, вписано вручную |

### 2.3. Как читать шкалу вероятности и воздействия (справочно, ниже на этом же листе)

Ниже основной таблицы (строки 27-34) есть справочные шкалы от автора шаблона — они не влияют на
расчёт, но помогают понять, что значат наши числа 0-1:

**Шкала вероятности** (столбец E там дан в процентах — наши значения F это и есть доли от 1, то есть
0,5 = «20-50%», примерно раз в год):

| Оценка (1-5) | Описание | Пример | % (соответствует нашей доле 0-1) |
|---|---|---|---|
| 1 | Очень низкая | Единичный случай за 10 лет | < 5% |
| 2 | Низкая | Раз в 2-3 года | 5-20% |
| 3 | Средняя | Раз в год | 20-50% |
| 4 | Высокая | Несколько раз в год | 50-80% |
| 5 | Очень высокая | Регулярно/постоянно | > 80% |

**Шкала силы воздействия** (для справки — у нас на листе воздействие тоже задано долей 0-1, эта
таблица показывает, что примерно значит каждый уровень в реальных последствиях):

| Оценка (1-5) | Описание | Финансовые потери | Репутационный ущерб | Юридические последствия |
|---|---|---|---|---|
| 1 | Очень низкое | < 500 BYN | Локальный | Нет |
| 2 | Низкое | 500-3000 BYN | Внутренний | Штраф до 25 б.в. |
| 3 | Среднее | 3000-20000 BYN | Региональный | Штраф 25-50 б.в. |
| 4 | Высокое | 20000-100000 BYN | Национальный | Штраф 50-100 б.в., проверка НЦЗПД |
| 5 | Очень высокое | > 100 000 BYN | Международный | Блокировка деятельности, судебные иски |

### 2.4. Таблица наших 9 рисков (сверь с тем, что видишь в Excel)

| № | Риск | Вероятность (F) | Воздействие (G) | Коэффициент (H=F×G) |
|---|---|---|---|---|
| 1 | Ветки feature/notation-bpmn-and-collab и feature/real-ide-app не смёржены к дате защиты | 0,5 | 0,9 | 0,45 |
| 2 | Нотации uml.class и idef0 (FR-NOT-06) не реализованы к сроку | 0,6 | 0,7 | 0,42 |
| 3 | Реальная БД Neon PostgreSQL и Redis не подключены и не проверены end-to-end | 0,4 | 0,5 | 0,20 |
| 4 | Единственный разработчик перегружен или заболевает перед защитой | 0,3 | 0,8 | 0,24 |
| 5 | Качество кода фоновых ИИ-агентов не соответствует ожиданиям при ручной проверке | 0,5 | 0,6 | 0,30 |
| 6 | AI-провайдер остаётся в режиме заглушки (StubAIProvider) | 0,6 | 0,3 | 0,18 |
| 7 | Хостинг-провайдер (Render/Netlify) недоступен или меняет бесплатные лимиты | 0,2 | 0,7 | 0,14 |
| 8 | Смешение трёх обязательств (лабы Часть 1/2 + курсовая + реальный продукт) в одном репозитории | 0,4 | 0,6 | 0,24 |
| 9 | Дата защиты курсовой работы смещается или уточняется поздно | 0,2 | 0,5 | 0,10 |

Самые приоритетные риски (по коэффициенту H) — №1 (несмёрженные ветки) и №2 (недоделанные нотации) —
именно на них стоит обратить внимание в первую очередь при защите.

### 2.5. Лист «Карта рисков» — как проверить диаграмму

1. Перейди на вкладку **«Карта рисков»**.
2. Диаграмма рассеяния находится примерно в верхней части листа, рядом с подписью «1 вариант» —
   это готовая сетка с осями «Оценка вероятности возникновения риска» (по горизонтали) и «Оценка силы
   воздействия риска» (по вертикали).
3. На этой сетке должно появиться **9 точек** — по одной на каждый риск из таблицы выше. Точки в
   правом верхнем углу — самые опасные риски (высокая вероятность И высокое воздействие).
4. Если точек не видно или их меньше 9 — нажми Ctrl+Alt+F9 ещё раз и подожди пару секунд.
5. Ниже на этом же листе есть ещё два дополнительных варианта сетки («2 вариант», «3 вариант») — это
   запасные визуализации от автора шаблона, диаграмма к ним не привязана, их можно игнорировать.

### 2.6. Что и как скриншотить

1. **Win+Shift+S**, выдели именно область с диаграммой рассеяния («1 вариант») вместе с подписями осей
   — не весь лист целиком, а конкретно график с точками.
2. Сохрани как PNG, назови `Карта рисков - скриншот.png`, положи в папку `risk-management\`.

**Подпись под рисунком:**
> Рисунок 2 — Карта рисков проекта DiagramCode: положение 9 идентифицированных рисков на матрице
> «вероятность × сила воздействия», построено автоматически по данным реестра рисков (лист «Таблица с
> рисками»); наиболее приоритетные риски — несмёрженные ветки разработки и нереализованные обязательные
> нотации (uml.class, idef0).

---

## 2.7. Проверка расчётов без Excel — независимо, через Python

Ты сказала, что пока не можешь сама открыть и посчитать в Excel — я пересчитала оба набора чисел
(и график Ганта/CPM, и коэффициенты рисков) отдельно, обычным Python-кодом, без всякого Excel. Оба
скрипта лежат в репозитории и дают ровно те же числа, что в таблицах выше:

- `pz-wbs-gantt\verify_cpm.py` — независимо пересчитывает метод критического пути (ES/EF/LS/LF/резерв/
  критический путь) для всех 12 работ; выдал ровно ту же таблицу и тот же критический путь
  (1→2→4→6→8→10→11→12, 39 дней), что описаны в разделе 1.3.
- `risk-management\verify_risk_scores.py` — независимо пересчитывает итоговый коэффициент риска
  (вероятность × воздействие) для всех 9 рисков; выдал ровно те же значения, что в разделе 2.4.

Если у тебя установлен Python — можешь запустить их сама (`python verify_cpm.py` из папки
`pz-wbs-gantt`, `python verify_risk_scores.py` из папки `risk-management`) и увидеть тот же результат
без Excel вообще. Это не заменяет скриншот самого Excel-файла для отчёта (в разделах 1.5 и 2.6 выше) —
но подтверждает, что цифры в файле верны, даже если открыть его пока не получается.

## 3. Если что-то не совпадает

Если после Ctrl+Alt+F9 цифры или график всё равно не совпадают с описанным выше — не пытайся исправлять
формулы вручную (легко случайно сломать структурную ссылку на `Таблица1`). Просто напиши мне, что именно
не так (лучше — со скриншотом), и я гляну, что могло съехать.
